% updates simulation initial states and parameters

%display_length=min(k_states,5);
%for i=1:display_length,
%x(max(CurrentState-5,0)+i)=str2num(get(StateValue(i),'String'));
%end

%display_length=min(k_params,5);
%for i=1:display_length,
 % param(max(CurrentState-5,0)+i)=str2num(get(ParamValue(i),'String'));
%end

t_final=str2num(get(simul_time_value,'String'));
